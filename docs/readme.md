A Jekyll Site to showcase my achievements

I attempted to implemnt Jekyll-Auth, however It is built to work on Heroku apps and my site runs on GitHub Pages. 

Also I always had an error saying: GET / raise NotFound, "#{request.request_method} #{request.path_info}" ^^^^^

Not sure what this meant but I don't want to encrypt the whole site anyway. 